# NeRF is version-fragile: the model mixes low-level tf ops with the Keras API,
# and the script uses the standalone `import keras` idiom (Keras 3), which
# resolves to Keras 3 only on TensorFlow 2.16+. Pins below reflect that family.
# If you re-run and regenerate the committed outputs, freeze these to the exact
# versions that reproduced them.
tensorflow>=2.16,<2.20
keras>=3.0,<4.0
numpy>=1.24
imageio>=2.31
tqdm>=4.66
matplotlib>=3.8

# Frozen study stack — NeRF over tiny_nerf_data. See README.
# Pins exatos (==) como no VAE: ranges abertos quebravam entre TF 2.16/Keras 3
# (import keras standalone) e numpy 2.x. Se re-rodar e regenerar outputs,
# congele nas versões exatas que reproduziram.
tensorflow==2.19.0
keras==3.8.0
numpy==1.26.4
imageio==2.36.1
tqdm==4.67.1
matplotlib==3.8.4
